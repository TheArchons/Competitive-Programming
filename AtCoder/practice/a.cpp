#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.sync_with_stdio(0); cin.tie(0);
    //freopen("a.input", "r", stdin); // for testing
    
    int a, b, c;
    cin >> a >> b >> c;

    string s;
    cin >> s;

    cout << a + b + c << " " << s << "\n";

    return 0;
}